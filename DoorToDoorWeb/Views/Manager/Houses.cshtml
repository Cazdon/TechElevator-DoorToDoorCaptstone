@section Title {Houses - Manager}
@section NavBar{
    <partial name="~/Views/Manager/Partials/ManagerNavBar.cshtml" />
}
@model ManagerHousesListViewModel
@using DoorToDoorLibrary.DatabaseObjects

<form asp-controller="Manager" asp-action="CreateHouse" method="post" id="manager-create-house">
    <label asp-for="CreatedHouse.Street"></label>
    <input asp-for="CreatedHouse.Street" type="text" />
    <span asp-validation-for="CreatedHouse.Street"></span>
    <label asp-for="CreatedHouse.City"></label>
    <input asp-for="CreatedHouse.City" type="text" />
    <span asp-validation-for="CreatedHouse.City"></span>
    <label asp-for="CreatedHouse.District"></label>
    <input asp-for="CreatedHouse.District" type="text" />
    <span asp-validation-for="CreatedHouse.District"></span>
    <label asp-for="CreatedHouse.ZipCode"></label>
    <input asp-for="CreatedHouse.ZipCode" type="text" />
    <span asp-validation-for="CreatedHouse.ZipCode"></span>
    <label asp-for="CreatedHouse.Country"></label>
    <input asp-for="CreatedHouse.Country" type="text" />
    <span asp-validation-for="CreatedHouse.Country"></span>

    <button>Create House</button>
    <span>
        @Html.ValidationMessage("invalid", new { @class = "field-validation-error" })
    </span>
</form>
<h2>My Houses:</h2>
@*<div class="container">
    <span>
        @Html.ValidationMessage("resetFailed", new { @class = "field-validation-error" })
    </span>
    @if ((TempData["resetSuccess"] != null) && ((bool)TempData["resetSuccess"]))
    {
        <span id="reset-success">Reset Password flag successfully set!</span>
    }
</div>*@

<div class="container">
    <ul id="manager-house-list">
        <li class="house-list-item">
            <div class="house-list-street">Street</div>
            <div class="house-list-city">City</div>
            <div class="house-list-district">District</div>
            <div class="house-list-country">Country</div>
            <div class="house-list-salesperson">Assigned Salesperson</div>
        </li>
        @{
            @*foreach (UserItem user in Model.Salespeople)
            {
                <li class="manager-list-item">
                    <div class="salespeople-list-first-name">@(user.FirstName)</div>
                    <div class="salespeople-list-last-name">@(user.LastName)</div>
                    <div class="salespeople-list-email">@(user.EmailAddress)</div>
                    <form asp-controller="Manager" asp-action="ResetPassword" method="post" id="manager-update-password-form@(user.Id)" class="salespeople-list-reset">
                        <input type="hidden" value="@(user.Id)" name="userID" />
                        <button>Reset Password</button>
                        <span>
                            @Html.ValidationMessage($"resetFailed{user.Id}", new { @class = "field-validation-error" })
                        </span>
                    </form>
                </li>
            }*@
        }
    </ul>
</div>